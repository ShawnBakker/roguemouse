/**
 * Re-export of the `DispatchTool` type signature from
 * `@roguemouse/schemas`. Type-only file. The runtime factory that
 * produces a `DispatchTool` lives in `./dispatcher.ts`.
 */
export type { DispatchTool } from "@roguemouse/schemas";
